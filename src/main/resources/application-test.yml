spring:
  data:
    mongodb:
      uri : ${MONGO_URI}
  redis:
    host: localhost
    port: 6379    
  mail:
    host: smtp.gmail.com
    port: 587
    username: rathorepradhuman2@gmail.com
    password: ${EMAIL_PASSWORD}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.localhost: localhost
      mail.smtp.from: rathorepradhuman2@gmail.com
      mail.debug: false

weatherapi:
  key : ${WEATHER_API_KEY:24232525}